# An example file to hold credentials for a remote mount point to test against.
# Currently only supports username + pass authentication. Set location of this
# by running functional_tests.py --credentials ./remote_credentials.txt
location: x.x.x.x/MountName
username: a_name
password: a_password